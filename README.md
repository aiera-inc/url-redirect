# url-redirect
A tiny static HTML page that forwards to any target URL without sending a Referer header. Useful when a destination site blocks or silently fails on cross-origin requests (e.g., opening external links from Retool apps).
